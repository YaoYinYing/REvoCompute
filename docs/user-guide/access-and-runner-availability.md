# Access and Runner Availability

Runner readiness and user access are separate concerns. A family can be `READY` while entitlement or transient scheduler capacity still prevents submission; use the operator readiness report and access APIs as separate signals.
