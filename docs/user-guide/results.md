# Results

Completed tasks expose authenticated result manifests and artifact downloads through the server API. Scientific result views consume those manifests and retain a generic download fallback.
