version: 1
collections:
  smoke:
    cases:
    - id: minimal-opendde
      task: opendde
      input: {files: [tests/data/json/opendde_tiny.json]}
      parameters: {num_samples: 1, num_steps: 10, num_cycles: 1, use_msa: false, use_template: false}
